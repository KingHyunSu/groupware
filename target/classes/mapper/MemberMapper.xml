<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace = "com.groupware.member.MemberDAO">
    	<resultMap type="CustomUserDetails" id="userInfo">
    		<result column="id" property="id"/>
    		<result column="pw" property="pw"/>
    		<result column="name" property="name"/>
    		<result column="authority" property="authority"/>
    		<association property="deptDto" javaType="DeptDTO">
    			<result column="dept_name" property="dept_name"/>
    		</association>
    		<association property="rankDto" javaType="RankDTO">
				<result column="rank_name" property="rank_name"/>    		
    		</association>
    	</resultMap>
    	
    	<insert id="join">
    		INSERT INTO
    			member(id,pw,name,dept_no,rank_no)
    		VALUES
    			(#{id},#{pw},#{name},#{dept_no},#{rank_no})
    	</insert>
    	
    	<select id="idCheck" resultType = "int" parameterType = "String">
    		SELECT COUNT(id)
    		FROM
    			member
    		WHERE
    			id = #{id}	
    	</select>
    	
    	<select id="login" resultMap="userInfo">
    		SELECT
    			id, pw, name, authority, dept_name, rank_name
    		FROM
    			member
    		JOIN
    			dept
    		ON
    			member.dept_no = dept.dept_no
    		JOIN
    			rank
    		ON
    			member.rank_no = rank.rank_no
    		WHERE
    			id = #{id}
    	</select>
    	
    	<select id="deptname" parameterType="int" resultType = "String">
    		SELECT
    			dept_name
    		FROM
    			dept
    		WHERE
    			dept_no = #{dept_no}
    	</select>
    	
    	<select id="rankname" parameterType="int" resultType = "String">
    		SELECT
    			rank_name
    		FROM
    			rank
    		WHERE
    			rank_no = #{rank_no}
    	</select>
    	
    	<select id = "showUser" resultType = "MemberDTO">
    		SELECT
    			member.num, member.id, member.name, dept.dept_name, rank.rank_name
    		FROM
    			member
    		JOIN
    			dept
    		ON
    			member.dept_no = dept.dept_no
    		JOIN
    			rank
    		ON
    			member.rank_no = rank.rank_no
    		WHERE
    			member.authority = 'anonymous'
    	</select>
    	
    	<select id="selectDept" parameterType = "MemberDTO" resultType = "MemberDTO">
    		SELECT
    			member.num, member.name, dept.dept_name, rank.rank_name
    		FROM
    			member
    		JOIN
    			dept
    		ON
    			member.dept_no = dept.dept_no
    		JOIN
    			rank
    		ON
    			member.rank_no = rank.rank_no
    		WHERE
    			member.dept_no = #{dept_no}
    	</select>
    	
    	<select id="selectName" parameterType = "MemberDTO" resultType = "MemberDTO">
    		SELECT
    			member.name, dept.deptname, rank.rankname
    		FROM
    			member
    		JOIN
    			dept
    		ON
    			member.dept = dept.deptno
    		JOIN
    			rank
    		ON
    			member.rank = rank.rankno
    		WHERE
    			member.name = #{name}
    	</select>
    </mapper>